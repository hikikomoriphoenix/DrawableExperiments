package marabillas.loremar.drawableexperiments

import android.os.Bundle
import android.support.v7.app.AppCompatActivity

/**
 * Demonstrates the use of Nine-patch drawable to display a message bubble.
 */
class MessageBubbleActivity : AppCompatActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_message_bubble)
    }
}